@model SportsPro.Models.Customers
@{
    ViewData["Title"] = "Index";
}

<h1>Get Customer</h1>

<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Registrations">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <select name="ddl" asp-for="CustomerId"  class="form-control" asp-items="ViewBag.CustomerId"></select>
            </div>
            <div class="form-group">
                <input type="submit" value="Select" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>


